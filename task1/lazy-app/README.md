In this lab I learned how to use code solitting in React. I used React.lazy() to load pages only when users opens them. Suspense component shows loading message while component is loading. Lazy loading helps make application faster because not all code loads at start. I created several pages and navigation between them.