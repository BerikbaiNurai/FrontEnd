In this lab I learned how to handles errors in React application. I created ErrorBoundary component using class component. It catches errors from child components and shows fallback UI. I wrapped errors from child components and shows fallback UI. I wrapped Suspense with ErrorBoundary to handle errors in lazy loaded components. When error happens, usersees message and can press "Try again".