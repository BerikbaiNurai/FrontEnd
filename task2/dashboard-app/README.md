SSR & SSG Performance

First Load: SSG is faster because page is pre-rendered.
Data Freshness: SSR always returns fresh data, SSG only updaes at rebuild.
Server Load: SSR hits server on every request, SSG server from CDN/cache.