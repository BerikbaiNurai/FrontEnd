Lab_04: 

Task1: Event Handling - Form & Validation

This project implements a registration form using React.  All inputs are controlled components using useState and onChange handles. Validation is performed on input change and on form submission. The form submission is handled using a synthetic event with preventDefault(). Validation errors are displayed below each input and a success message is shown after valid submission.